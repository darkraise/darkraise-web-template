import { defaultMessages } from "../i18n/defaultMessages"
import type { UiLocaleDefinition } from "../i18n/types"

export const en = {
  locale: "en",
  label: "English",
  dir: "ltr",
  messages: defaultMessages,
} satisfies UiLocaleDefinition
